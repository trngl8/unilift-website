{% extends 'layout.html.twig' %}

{% block main %}
    <h1 class="mb-4">{{ page.title }}</h1>

    <div class="row">

        <div class="col-md-6">
            <p>{{ page.description }}</p>
            <div>{{ page.text|raw }}</div>
        </div>
    </div>
{% endblock %}
